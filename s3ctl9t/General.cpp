#include <iostream>
using namespace std;
class General {
public:
    void EntKb(char* bufA, char* bufB, char* bufC, char* bufD) {
      
/*
        cout << "Enter string of values A (asiydasf...)" << endl;
        cin >> bufA;
       
          std::cout << "Enter string of values B" << std::endl;
          std::cin.getline(bufB, 24);
          ar->bi = strlen(bufB);
          ar->b = new char[ar->bi];
          ar->b = bufB;
          std::cout << "Enter string of values C" << std::endl;
          std::cin.getline(bufC, 24);
          ar->ci = strlen(bufC);
          ar->c = new char[ar->ci];
          ar->c = bufC;
          std::cout << "Enter string of values D" << std::endl;
          std::cin.getline(bufD, 24);
          ar->di = strlen(bufD);
          ar->d = new char[ar->di];
          ar->d = bufD;
         */ 
    }












};